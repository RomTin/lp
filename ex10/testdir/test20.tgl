// 処理系のテスト２０
// 異常ケース９: 真ん中の引数の型が宣言と定義で異なる

real move(int x, int y, real z);

void main()
{
  int log = 7;
  log = log * 3;
}

// ２つ目の引数の型が宣言時と異なるのでエラー
real move(int x, real y, real z)
{ 
  return  x + y + z;
}
