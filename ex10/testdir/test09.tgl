// 処理系のテスト９: 引数の型変換

real still(real x, int y);

real still(real home, int away)
{
  return home + away;	// １回目は 6.0、２回目は 4.8 が返る
}

void main()
{
  int log = 3;
  real log2 = 2.8;
  real x;

  x = still(log,log);	// x = 6.0

  x = still(log2,log2);	// x = 4.8
}
