// if文のテスト６： if と １つの elsif．最初の条件が偽で二つ目の条件が真のとき
// 複雑な条件を含む

int x = -10;
int y = 3;
int z = 10;
y = 2*(y+5)%9;	// y = 7
if (z < 10)  {
  // ここには来ない
  x = x*100;
  z=y+z;
}
elsif (z >= 10 && (!(x > 0) || y < 3))  {
  // ここに来る
  x = x*-100;	// x = 1000;
  z = -y+z;	// z = 3;
}
z = 5*y+z;	// z = 38

